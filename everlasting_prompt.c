#include "thomas_shellby.h"

/* 
 * This is the function that causes the CLI for the shell program to display
 * We called it everlasting prompt because the point of it is to keep the CLI prompt on screen
 * previously called programs @i_print and @prompt_ly were used here
 */

void prompt_ly(void)
{
	i_print("shellby$ ");
}
